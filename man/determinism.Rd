% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/determinism.R
\name{determinism}
\alias{determinism}
\alias{stable_order}
\alias{hash_object}
\alias{hash_file}
\alias{with_seed}
\title{Determinism Helpers}
\usage{
stable_order(df, cols)

hash_object(x)

hash_file(path)

with_seed(seed, expr)
}
\arguments{
\item{df}{Data frame to sort.}

\item{cols}{Character vector of column names to sort by.}

\item{x}{R object to hash.}

\item{path}{File path to hash.}

\item{seed}{Random seed (integer).}

\item{expr}{Expression to evaluate with the seed set.}
}
\value{
\itemize{
\item \code{stable_order()}: The data frame sorted by \code{cols} with deterministic NA ordering.
\item \code{hash_object()}: Character string (hexadecimal digest).
\item \code{hash_file()}: Character string (hexadecimal digest).
\item \code{with_seed()}: The result of evaluating \code{expr}.
}
}
\description{
Functions for ensuring reproducible, deterministic behavior across
platforms and sessions.
}
\details{
\subsection{stable_order}{

Sorts data frame rows by specified columns with consistent NA handling
(NAs sorted to end). Ensures reproducible row order across platforms.
}

\subsection{hash_object}{

Uses SHA-256 via \code{digest::digest()} with \code{serialize = TRUE} and
\code{ascii = TRUE} for cross-platform stability.
}

\subsection{hash_file}{

Uses SHA-256 via \code{digest::digest()} with \code{file = TRUE} for consistent
file hashing.
}

\subsection{with_seed}{

Temporarily sets RNG seed, evaluates expression, and restores previous
RNG state even on error.
}
}
\examples{
df <- data.frame(x = c(3, 1, NA, 2), y = c("a", "b", "c", "d"))
stable_order(df, "x")

hash_object(list(a = 1, b = 2))

\dontrun{
hash_file("myfile.csv")
}

with_seed(123, rnorm(5))

}
